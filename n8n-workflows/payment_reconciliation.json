{
  "name": "BD Land - Payment Reconciliation & Dakhila Automation",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "payment-reconciled",
        "responseMode": "onReceived",
        "options": {}
      },
      "name": "Payment Webhook Ingress",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [250, 300]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT * FROM \"TaxRecord\" WHERE \"trxId\" = $1 AND \"status\" = 'VERIFIED';",
        "additionalFields": {
          "queryParams": "={{$json[\"body\"][\"trxId\"]}}"
        }
      },
      "name": "Verify against PostgreSQL",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2,
      "position": [480, 300]
    }
  ],
  "connections": {}
}
